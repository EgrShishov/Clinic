<UserControl x:Class="AdminApp.View.OfficesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:vm="clr-namespace:AdminApp.ViewModel"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AdminApp.View"
             mc:Ignorable="d">

    <UserControl.DataContext>
        <vm:OfficesViewModel/>
    </UserControl.DataContext>

    <Grid Background="#F4F6F9">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header with Create Button -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,10">
            <TextBlock Text="Offices" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Add New Office" 
                    Width="150" 
                    Height="30" 
                    Margin="20,0,0,0"
                    VerticalAlignment="Center"
                    Command="{Binding AddOfficeCommand}"
                    Style="{StaticResource RoundButton}"/>
        </StackPanel>

        <!-- Offices List -->
        <ScrollViewer Grid.Row="1">
            <ItemsControl ItemsSource="{Binding Offices}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" 
                                   VerticalAlignment="Top"
                                   HorizontalAlignment="Center"
                                   ItemWidth="250" 
                                   ItemHeight="200"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="White" 
                                BorderBrush="#CCCCCC" 
                                BorderThickness="1"
                                CornerRadius="10"
                                Margin="10"
                                Padding="10">
                            <StackPanel>
                                <Image Source="{Binding PhotoUrl}" 
                                       Width="100" 
                                       Height="100" 
                                       HorizontalAlignment="Center" 
                                       Margin="0,0,0,10"/>
                                <TextBlock Text="{Binding Location}" 
                                           FontWeight="Bold" 
                                           FontSize="16" 
                                           TextAlignment="Center"/>
                                <TextBlock Text="{Binding ContactNumber}" 
                                           Foreground="#666"
                                           FontSize="14" 
                                           TextAlignment="Center"
                                           Margin="0,5,0,0"/>
                                <StackPanel Orientation="Horizontal" 
                                            HorizontalAlignment="Center" 
                                            Margin="0,10,0,0">
                                    <Button Content="Edit" 
                                            Command="{Binding DataContext.EditOfficeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource RoundButton}"
                                            Margin="5,0"/>
                                    <Button Content="Delete" 
                                            Command="{Binding DataContext.DeleteOfficeCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource RoundButton}"
                                            Margin="5,0"/>
                                    <Button Content="Details" 
                                            Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                            CommandParameter="{Binding}"
                                            Style="{StaticResource RoundButton}"
                                            Margin="5,0"/>
                                </StackPanel>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
    
</UserControl>
